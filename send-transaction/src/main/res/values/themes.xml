<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Theme -->
    <style name="Theme.PenguinPay" parent="Theme.MaterialComponents.Light.NoActionBar.Bridge">
        <item name="android:windowBackground">@android:color/white</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorPrimary">@color/primary_main</item>
        <item name="colorAccent">@color/accent_main</item>
        <item name="colorOnPrimary">@android:color/white</item>

        <item name="android:statusBarColor">@color/primary_dark</item>

        <!-- Apply InputLayout Styles -->
        <item name="textInputStyle">@style/Widget.PenguinPay.TextInputLayout</item>

        <!-- Apply Button Styles -->
        <item name="materialButtonStyle">@style/Widget.PenguinPay.Button</item>
    </style>
</resources>
